<div style="margin-left: 200px; padding: 15px;">
  <p class="alert alert-success text-black"><%= notice %></p>

  <h1>Cars</h1>
   <div class="py-2">
    <%= link_to "Adicionar carro", new_car_path, class:"btn btn-success" %>
  </div>
  <div class="row col-12">
    <% @cars.each do |car| %>
      <div class="col-4 mb-3">
        <%= render car %>
        <p>
          <div class="mr-2 my-3">
            <%= link_to "Editar Carro", edit_car_path(car), class: "btn btn-primary" %>
          </div>
        </p>
<%= link_to "Excluir Carro", car, method: :delete, data: { confirm: 'Você tem certeza?' }, class: "btn btn-danger" %>      </div>
    <% end %>
  </div>
 
</div>
