<%= form_with(model: servico) do |form| %>
  <% if servico.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(servico.errors.count, "error") %> prohibited this servico from being saved:</h2>

      <ul>
        <% servico.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label :tipoDeServico, style: "display: block" %>
<%= form.text_field :tipoDeServico %>

<%= form.label :car_id, style: "display: block" %>
<%= form.collection_select(:car_id, Car.all, :id, :ano, prompt: true) %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
